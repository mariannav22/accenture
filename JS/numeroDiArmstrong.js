function isArmstrong(value) {
    if ( !Number.isInteger(value) || value < (0)) {
        return undefined;
    }
    variabile= value.toString()
    let sum = 0
    for (let i = 0; i < value.length; i++)
        if (sum = Math.pow(value.length)) {
            return true;
        } else {
            return false;
        }

}


console.log(isArmstrong(32)===3**2+2**2);
